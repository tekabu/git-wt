[package]
name = "git-wt"
version = "1.3.14"
edition = "2021"
description = "Create git worktrees in sibling directories named <repo>-<branch>"

[dependencies]
stanza = "0.5.2"

[profile.release]
opt-level = "z"
lto = true
strip = true
codegen-units = 1
panic = "abort"
